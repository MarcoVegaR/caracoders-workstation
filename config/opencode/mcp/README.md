# MCP configuration

MCP v1: filesystem, playwright, context7, postgres.

Shell MCP is intentionally not part of this standard.

Filesystem access is scoped by default. All-HOME access requires explicit consent. PostgreSQL MCP uses development credentials only.
