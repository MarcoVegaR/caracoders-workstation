# pgAdmin local template

Runs pgAdmin as a local container for development inspection.

```bash
cp .env.example .env
docker compose up -d
```

Open `http://localhost:5050`. Use example credentials only for local development.
