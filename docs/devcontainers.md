# Dev Containers

Dev Containers are mandatory for projects. The host provides Docker, VS Code and extension; each project provides Dockerfile, Compose, devcontainer.json, `.env.example`, AGENTS and Makefile/Justfile pattern.
